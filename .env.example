# Local copies remain non-indexable. Set both deployment values explicitly for a reviewed release.
PORTFOLIO_DEPLOYMENT_CONTEXT=local
NEXT_PUBLIC_SITE_INDEXING=deny

# Exact HTTPS origin used for canonical metadata, structured data, sitemap, and robots output.
# Do not include credentials, a path, query parameters, or a fragment.
NEXT_PUBLIC_SITE_URL=
